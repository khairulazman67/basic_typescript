"use strict";
describe('typeData', function () {
    it('should be type data ', function () {
        const name = "Khairul Azman";
        let balance = 1000000;
        console.info(name);
        console.info(balance);
    });
});
